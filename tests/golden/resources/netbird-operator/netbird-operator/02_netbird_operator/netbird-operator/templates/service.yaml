apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: netbird-operator
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: netbird-operator
    app.kubernetes.io/version: v0.7.0
    helm.sh/chart: netbird-operator-0.7.0
  name: netbird-operator-metrics
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/instance: netbird-operator
    app.kubernetes.io/name: netbird-operator
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: netbird-operator
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: netbird-operator
    app.kubernetes.io/version: v0.7.0
    helm.sh/chart: netbird-operator-0.7.0
  name: netbird-operator-webhook-service
spec:
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    app.kubernetes.io/instance: netbird-operator
    app.kubernetes.io/name: netbird-operator
  type: ClusterIP
